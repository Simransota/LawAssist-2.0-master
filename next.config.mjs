/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'res.cloudinary.com', 'assets.aceternity.com', 'cdn3.vectorstock.com']
    }
};

export default nextConfig;
